# image-carousel
Simple image carousel made with CSS, HTML and vanilla JS. Use of CSS transitions to make a sliding animation, and JS to set an interval of 5 seconds between each transition. Can also go to the next image, move back an image, and switch to any image using the bar below the carousel.
